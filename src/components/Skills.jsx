import React from 'react';
import { motion } from 'framer-motion';

const Skills = () => {
    // Skills are now integrated into the About section's Terminal component 
    // to maintain the "High Tech" feel and reduce scrolling fatigue.
    // This file acts as a placeholder if we want to expand later, 
    // or we can remove it. For now, we will return null or a minimal separator.
    return null; 
};

export default Skills;
